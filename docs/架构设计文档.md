# 链环系统生�?V1.0 �?架构设计文档

> **版本**: 1.0 | **日期**: 2026-06-15 | **状�?*: 待评�?> **技术负责人**: CTO / @go / @vue

---

## 目录

1. [系统概述](#1-系统概述)
2. [技术选型决策](#2-技术选型决策)
3. [系统架构](#3-系统架构)
4. [模块设计](#4-模块设计)
5. [数据库设计](#5-数据库设�?
6. [API 接口规范](#6-api-接口规范)
7. [前端架构](#7-前端架构)
8. [安全设计](#8-安全设计)
9. [部署架构](#9-部署架构)
10. [开发路线图](#10-开发路线图)
11. [开发规范](#11-开发规�?
12. [验收标准](#12-验收标准)

---

## 1. 系统概述

### 1.1 定位

链环系统是一个基于区块链技术的供应链溯源管理平台，实现产品从生产到销售的全链路透明追溯�?
### 1.2 核心目标

| 目标 | 说明 |
|------|------|
| **可信溯源** | 利用区块链不可篡改特性，确保溯源信息真实可信 |
| **全链协同** | 打通生产、质检、仓储、物流各环节数据 |
| **公开查询** | 消费者可通过二维码查询产品完整生命周�?|
| **权限可控** | 多角�?RBAC 权限体系，企业数据隔�?|

### 1.3 MVP 范围

```
MVP 范围
├── 用户与权限管�?(登录/注册/角色/企业)
├── 产品溯源管理 (产品/批次/记录/追溯�?二维�?
├── 供应链管�?(供应�?订单/仓储/物流)
├── 区块链集�?(模拟�?区块浏览�?合约管理)
└── 系统管理 (配置/日志/通知/仪表�?文件上传)
```

---

## 2. 技术选型决策

### 2.1 选型全景

| 层次 | 选型 | 版本 | 决策理由 |
|------|------|------|----------|
| **后端框架** | GoFrame v2 | latest | 国内企业�?Go 框架首选，自带 ORM/RBAC/配置管理, 社区活跃, 中文文档完备 |
| **前端框架** | Vue 3 + Element Plus | 3.4+/2.5+ | 成熟度高、生态完善、与 Element Admin 模板兼容 |
| **状态管�?* | Pinia | 2.1+ | Vue3 官方推荐，TypeScript 友好 |
| **构建工具** | Vite | 5+ | 极�?HMR、开箱即�?|
| **数据�?* | MySQL 8.0 | 8.0+ | 稳定可靠，事务支持好 |
| **缓存** | Redis | 6+ | Token 缓存、会话管理、热点数�?|
| **图表** | ECharts + vue-echarts | 5.4+ | 开源最强可视化�?|
| **二维�?* | qrcode.js | 1.5+ | 轻量无依�?|
| **API 风格** | RESTful JSON | �?| 前后端分离标准，通用性强 |

### 2.2 为什么不用？

| 被否决方�?| 原因 |
|-----------|------|
| gRPC | MVP 阶段 HTTP/1.1 足以胜任，gRPC 增加调试和部署复杂度 |
| TypeScript | MVP 阶段 JS 开发效率更高，TS 可在后续版本引入 |
| Vue 2 | 已停止维护，Vue3 是大势所�?|
| 真实区块链网�?| MVP 先模拟链验证业务流程，后续对接真实链 |

---

## 3. 系统架构

### 3.1 分层架构�?
```
┌─────────────────────────────────────────────────────────────�?�?                   客户�?(Browser)                           �?�?          Vue 3 + Element Plus + Pinia + Axios              �?├─────────────────────────────────────────────────────────────�?�?                        Nginx                                �?�?            反向代理 / 静态资�?/ HTTPS 卸载                   �?├─────────────────────────────────────────────────────────────�?�?                  后端 API (GoFrame v2)                       �?�? ┌──────────┬──────────┬──────────┬──────────┬──────────�?  �?�? �?用户权限  �?产品溯源  �?供应链管�?�?区块链集�?�?系统管理  �?  �?�? �?Controller│Controller│Controller│Controller│Controller�?  �?�? ├──────────┼──────────┼──────────┼──────────┼──────────�?  �?�? �? Service  �? Service �? Service �? Service �? Service �?  �?�? ├──────────┴──────────┴──────────┴──────────┴──────────�?  �?�? �?                   DAO / ORM                           �?  �?�? └──────────────────────┬────────────────────────────────�?  �?├──────────────────────────┼──────────────────────────────────�?�?           ┌─────────────┴─────────────�?                    �?�?           �?       MySQL 8.0          �?                    �?�?           └─────────────┬─────────────�?                    �?�?           ┌─────────────┴─────────────�?                    �?�?           �?        Redis 6+          �?                    �?�?           └───────────────────────────�?                    �?└─────────────────────────────────────────────────────────────�?```

### 3.2 后端分层说明 (GoFrame 标准模式)

| 层级 | 职责 | 目录 |
|------|------|------|
| **Controller** | 请求/响应处理、参数校验、路由绑�?| `internal/controller/*` |
| **Service** | 业务逻辑接口定义 | `internal/service/*.go` |
| **Logic** | 业务逻辑实现 | `internal/logic/*` |
| **Model** | 数据结构定义(输入/输出/实体) | `internal/model/*.go` |
| **DAO** | 数据访问�?| `internal/dao/*` |

### 3.3 前端分层说明

| 层级 | 职责 | 目录 |
|------|------|------|
| **Views** | 页面组件 (每个功能模块一个子目录) | `views/*/` |
| **Layouts** | 主布局组件(侧边�?头部+内容�? | `layouts/` |
| **API** | Axios 封装，统一请求拦截 | `api/` |
| **Router** | 路由定义 (19 条路�? | `router/` |
| **Stores** | Pinia 全局状�?| `stores/` |

---

## 4. 模块设计

### 4.1 用户与权限模�?
**数据�?*:
```
Login Request �?Controller.Login �?Service.Login �?DB(用户�?
                                           �?                                     生成 JWT Token
                                           �?                                 返回 Token + 用户信息
                                           �?                                 前端存储 Token �?Pinia
```

**关键设计**:
- JWT 认证，Token �?Redis 做主动失�?- 密码 bcrypt 加密存储 (config.yaml 中配�?salt)
- 角色权限�? `super_admin > admin > inspector > user` 分级
- 企业数据隔离：查询条件追�?`enterprise_id`

**接口清单**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/user/login` | 登录 | �?|
| POST | `/api/user/register` | 注册 | �?|
| POST | `/api/user/logout` | 登出 | �?|
| GET | `/api/user/info` | 获取用户信息 | �?|
| GET | `/api/user/list` | 用户列表 | �?|
| PUT | `/api/user/update` | 更新用户 | �?|
| DELETE | `/api/user/delete/:id` | 删除用户 | �?|
| GET | `/api/user/roles` | 角色列表 | �?|
| POST | `/api/user/role` | 创建角色 | �?|
| PUT | `/api/user/role` | 更新角色 | �?|
| DELETE | `/api/user/role/:id` | 删除角色 | �?|
| GET | `/api/user/enterprise` | 企业列表 | �?|
| POST | `/api/user/enterprise` | 创建企业 | �?|
| PUT | `/api/user/enterprise` | 更新企业 | �?|

### 4.2 产品溯源模块

**数据�?*:
```
企业创建产品 �?生成批次 �?录入溯源记录(生产/加工/质检/物流)
                                     �?                             可选上�?存证)
                                     �?                             生成二维�?含产品ID)
                                     �?                         消费者扫�?�?公开查询追溯�?```

**溯源链设�?*:
```
产品信息 ←── 批次信息 ←── 溯源记录1 (生产)
                            �?                        溯源记录2 (加工)
                            �?                        溯源记录3 (质检)
                            �?                        溯源记录4 (物流)
                            �?                       打包上链(可�?
```

**接口清单**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/trace/product` | 创建产品 | �?|
| PUT | `/api/trace/product` | 更新产品 | �?|
| DELETE | `/api/trace/product/:id` | 删除产品 | �?|
| GET | `/api/trace/product/:id` | 产品详情 | �?|
| GET | `/api/trace/product/list` | 产品列表 | �?|
| POST | `/api/trace/batch` | 创建批次 | �?|
| GET | `/api/trace/batch/:id` | 批次详情 | �?|
| GET | `/api/trace/batch/list` | 批次列表 | �?|
| POST | `/api/trace/record` | 创建溯源记录 | �?|
| GET | `/api/trace/record/:id` | 溯源记录详情 | �?|
| GET | `/api/trace/record/list/:productId` | 溯源记录列表 | �?|
| GET | `/api/trace/chain/:productId` | 追溯�?| �?|
| GET | `/api/trace/qrcode/:productId` | 生成二维�?| �?|
| GET | `/api/trace/public/query` | 公开查询 | �?|

### 4.3 供应链管理模�?
**数据�?*:
```
供应商注�?�?资质审核 �?下单 �?入库 �?物流发货 �?确认收货
                                    �?                             仓储记录存证
```

**接口清单**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| POST | `/api/supply/supplier` | 创建供应�?| �?|
| PUT | `/api/supply/supplier` | 更新供应�?| �?|
| GET | `/api/supply/supplier/:id` | 供应商详�?| �?|
| GET | `/api/supply/supplier/list` | 供应商列�?| �?|
| DELETE | `/api/supply/supplier/:id` | 删除供应�?| �?|
| POST | `/api/supply/order` | 创建订单 | �?|
| PUT | `/api/supply/order` | 更新订单 | �?|
| GET | `/api/supply/order/:id` | 订单详情 | �?|
| GET | `/api/supply/order/list` | 订单列表 | �?|
| DELETE | `/api/supply/order/:id` | 删除订单 | �?|
| POST | `/api/supply/warehouse/in` | 入库 | �?|
| POST | `/api/supply/warehouse/out` | 出库 | �?|
| GET | `/api/supply/warehouse/stock` | 库存查询 | �?|
| POST | `/api/supply/logistics` | 创建物流记录 | �?|
| GET | `/api/supply/logistics/:orderId` | 物流跟踪 | �?|

### 4.4 区块链集成模�?
**数据�?(模拟�?**:
```
上链请求 �?模拟生成交易哈希 �?模拟生成区块 �?返回存证结果
                                           �?                                   交易记录写入 trace_record.tx_hash
```

**注意**: MVP 阶段使用内存模拟区块链，不依赖真实节点。数据存证的结果记录到数据库，提供区块浏览器查看�?
**接口清单**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/blockchain/blocks` | 区块列表 | �?|
| GET | `/api/blockchain/blocks/:hash` | 区块详情 | �?|
| GET | `/api/blockchain/transactions` | 交易列表 | �?|
| GET | `/api/blockchain/transactions/:txId` | 交易详情 | �?|
| POST | `/api/blockchain/contract` | 部署合约 | �?|
| GET | `/api/blockchain/contract/:address` | 合约详情 | �?|
| GET | `/api/blockchain/contracts` | 合约列表 | �?|
| POST | `/api/blockchain/data/upload` | 数据上链 | �?|
| POST | `/api/blockchain/data/verify` | 数据验证 | �?|

### 4.5 系统管理模块

**接口清单**:

| 方法 | 路径 | 说明 | 鉴权 |
|------|------|------|------|
| GET | `/api/system/dashboard` | 仪表盘数�?| �?|
| GET | `/api/system/config` | 配置列表 | �?|
| PUT | `/api/system/config` | 更新配置 | �?|
| GET | `/api/system/logs` | 操作日志 | �?|
| DELETE | `/api/system/logs/:id` | 删除日志 | �?|
| GET | `/api/system/notifications` | 通知列表 | �?|
| PUT | `/api/system/notifications/:id/read` | 标记已读 | �?|
| POST | `/api/upload` | 文件上传 | �?|

---

## 5. 数据库设�?
### 5.1 ER 关系总览

```
sys_user (1) ──── (N) enterprise
sys_user (N) ──── (1) sys_role

enterprise (1) ──── (N) product
product (1) ──────── (N) batch
product (1) ──────── (N) trace_record
batch (1) ────────── (N) trace_record

supplier (1) ─────── (N) order_info
product (1) ──────── (N) order_info
order_info (1) ───── (N) warehouse_record
order_info (1) ───── (N) logistics_record

contract (独立�?
sys_config (独立�?
sys_log (独立�?
notification (N) ──── (1) sys_user
```

### 5.2 表结构总览 (10 张表)

| 表名 | 说明 | 核心字段 | 行数预估 |
|------|------|---------|---------|
| sys_user | 用户�?| username, password, role, enterprise_id | 100+ |
| sys_role | 角色�?| name, code, permissions(JSON) | 4 |
| enterprise | 企业�?| name, code, contact, status | 20+ |
| product | 产品�?| name, category, spec, enterprise_id | 500+ |
| batch | 批次�?| batch_no, product_id, quantity | 5000+ |
| trace_record | 溯源记录�?| product_id, record_type, tx_hash | 5W+ |
| supplier | 供应商表 | name, qual_level, status | 100+ |
| order_info | 订单�?| order_no, supplier_id, total_amount | 1000+ |
| warehouse_record | 仓储记录�?| order_id, op_type(in/out), quantity | 5000+ |
| logistics_record | 物流记录�?| order_id, node_name, location | 5000+ |
| contract | 合约�?| address, name, abi, deploy_tx | 10+ |
| sys_config | 系统配置�?| key, value, desc | 5 |
| sys_log | 操作日志�?| user_id, type, action, ip | 10W+ |
| notification | 通知�?| user_id, title, content, is_read | 1000+ |

### 5.3 索引策略

所有表均配�?

- 主键索引: `id` (AUTO_INCREMENT)
- 外键关联索引: `enterprise_id`, `product_id`, `batch_id`, `order_id`, `user_id`
- 业务查询索引: `username`(唯一), `batch_no`, `order_no`, `tx_hash`, `status`, `created_at`
- 复合索引: 视查询频率后续补�?
---

## 6. API 接口规范

### 6.1 通用规范

**请求格式**: `Content-Type: application/json` �?`multipart/form-data`(上传)

**响应格式**:

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

错误�?

```json
{
  "code": 401,
  "message": "未授权访�?
}
```

**错误码约�?*:

| 范围 | 含义 |
|------|------|
| 0 | 成功 |
| 400-499 | 客户端错�?参数校验/鉴权) |
| 500-599 | 服务端错�?|

**鉴权方式**: `Authorization: Bearer <jwt_token>`

### 6.2 分页规范

请求参数: `?page=1&pageSize=20`
响应数据:

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [...],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}
```

### 6.3 URL 命名规范

```
/{module}/{resource}[/{id}][/{action}]
/api/trace/product/list
/api/trace/product/:id
/api/trace/product/:id/qrcode
```

---

## 7. 前端架构

### 7.1 路由结构

```
/login                    �?Login.vue          (公开)
/dashboard                �?Dashboard.vue       (需登录)
/user                     �?UserList.vue
/user/role                �?RoleList.vue
/enterprise               �?EnterpriseList.vue
/product                  �?ProductList.vue
/product/detail/:id       �?ProductDetail.vue
/batch                    �?BatchList.vue
/trace-record             �?TraceRecordList.vue
/trace/chain/:productId   �?TraceChain.vue
/supplier                 �?SupplierList.vue
/order                    �?OrderList.vue
/warehouse                �?WarehouseList.vue
/logistics                �?LogisticsList.vue
/blockchain               �?BlockList.vue
/blockchain/tx            �?TransactionList.vue
/blockchain/contract      �?ContractList.vue
/system/config            �?ConfigList.vue
/system/log               �?LogList.vue
```

### 7.2 状态管�?(Pinia)

```
stores/user.js
├── token           �?JWT Token
├── userInfo        �?当前登录用户信息
├── login()         �?登录 action
├── logout()        �?登出 action
└── fetchUserInfo() �?获取用户信息 action
```

### 7.3 请求拦截

```
Axios 拦截�?├── request: 自动注入 Authorization header
└── response: 401 自动跳转登录�?```

---

## 8. 安全设计

### 8.1 认证与授�?
| 措施 | 实现 |
|------|------|
| 密码存储 | bcrypt 哈希 + salt (config.yaml 配置) |
| Token | JWT, 24 小时过期 |
| Token 失效 | Redis 黑名单机�?|
| API 鉴权 | 路由中间�?`AuthMiddleware` |
| RBAC | 角色-权限 JSON 配置 |

### 8.2 数据安全

| 措施 | 实现 |
|------|------|
| 注入防护 | GoFrame ORM 参数化查�?|
| XSS | Vue 自动转义 |
| CORS | 中间件配置允许域�?|
| 文件上传 | 限制类型/大小 |

### 8.3 企业数据隔离

所有查询数据接口需追加 `enterprise_id` 过滤条件，确保跨企业数据不可见�?
---

## 9. 部署架构

### 9.1 开发环�?
```
┌──────────────�?    ┌──────────────�?    ┌──────────────�?�? Vue Dev Server �?    �? Go API Server �?    �?   MySQL      �?�? :3000          │────▶│  :8099          │────▶│    :3306      �?�? HMR Enable     �?    �? Hot Reload     �?    �?              �?└──────────────�?    └──────────────�?    └──────────────�?```

### 9.2 生产环境

```
Nginx (80/443) ──�?GoFrame (8099) ──�?MySQL (3306)
       �?                             Redis (6379)
       └── 静态资�?(dist/)
```

---

## 10. 开发路线图

### 10.1 迭代计划

| 迭代 | 阶段 | 时间 | 内容 |
|------|------|------|------|
| **V1.0** | MVP | 当前 | 完整功能链路 + 数据�?+ 页面 |
| **V1.1** | 功能增强 | TBD | 权限路由拦截、搜�?筛选优�?|
| **V1.2** | 真实链接�?| TBD | 对接 Hyperledger Fabric / FISCO BCOS |
| **V1.3** | 部署上线 | TBD | Docker �?+ CI/CD + 域名/证书 |
| **V2.0** | 智能升级 | TBD | 数据分析看板 + 预警系统 + 移动�?|

### 10.2 当前迭代分工

| 任务 | 负责�?| 预计工时 | 优先�?|
|------|--------|---------|--------|
| 后端: 用户模块 Controller/Logic | @go | 3h | P0 |
| 后端: 溯源模块 Controller/Logic | @go | 4h | P0 |
| 后端: 供应链模�?Controller/Logic | @go | 3h | P0 |
| 后端: 区块链模�?Controller/Logic | @go | 2h | P0 |
| 后端: 系统模块 Controller/Logic | @go | 2h | P0 |
| 后端: JWT 鉴权中间�?| @go | 1h | P0 |
| 后端: DAO �?+ Model 定义 | @go | 2h | P0 |
| 后端: 数据�?Schema + 初始化数�?| @go | 1h | P0 |
| 前端: 项目脚手�?+ Layout + Router | @vue | 2h | P0 |
| 前端: 登录/仪表�?| @vue | 2h | P0 |
| 前端: 用户管理页面 | @vue | 2h | P0 |
| 前端: 溯源管理页面�?| @vue | 3h | P0 |
| 前端: 供应链页面组 | @vue | 3h | P0 |
| 前端: 区块链页面组 | @vue | 2h | P0 |
| 前端: 系统管理页面 | @vue | 1h | P0 |
| 前端: Axios 封装 + Pinia Store | @vue | 1h | P0 |
| 测试: 后端 API 测试 | @tester | 3h | P1 |
| 测试: 前端 E2E 测试 | @tester | 2h | P1 |

---

## 11. 开发规�?
### 11.1 Git 规范

```
分支策略:
  main        �?稳定版本
  develop     �?开发主分支
  feature/*   �?功能分支
  hotfix/*    �?紧急修�?
提交信息: feat|fix|docs|refactor|test(模块): 描述
示例: feat(trace): 添加产品溯源二维码生成接�?```

### 11.2 代码规范

**Go**:
- 遵循 GoFrame 编码规范
- 错误处理: 所�?DB 操作必须检�?error
- Model/Service/Controller 三层严格分离
- 接口优先，Logic 层实�?Service 接口

**Vue/JS**:
- 组件使用 `<script setup>` + Composition API
- API 请求统一�?`api/index.js`
- 页面组件放在对应模块目录�?- 通用组件抽取�?`components/`

### 11.3 日志规范

```
格式: [级别] [模块] [操作] - 描述 - 耗时
示例: [INFO] [trace] [CreateProduct] - success - 15ms
输出: 运行时日�?+ sys_log 数据�?```

---

## 12. 验收标准

### 12.1 功能验收

| 检查项 | 标准 |
|--------|------|
| 用户登录/注册 | 账号密码可正常登�?|
| 角色管理 | 支持创建/编辑/删除角色 |
| 企业信息 | 企业CRUD正常，数据隔离生�?|
| 产品管理 | 产品增删改查正常 |
| 批次管理 | 批次关联产品正常 |
| 溯源记录 | 四种记录类型均可录入查询 |
| 追溯�?| 按产品ID可查完整追溯链路 |
| 二维�?| 二维码可生成可扫码访�?|
| 供应商管�?| CRUD正常 |
| 订单管理 | CRUD + 状态流转正�?|
| 仓储管理 | 入库/出库/库存查询正常 |
| 物流追踪 | 物流节点可录入可追踪 |
| 区块浏览�?| 正常展示模拟区块数据 |
| 合约管理 | 合约增删改查正常 |
| 数据上链 | 溯源记录可模拟上�?|
| 数据验证 | 链上数据可验�?|
| 仪表�?| 数据看板正常展示 |
| 配置管理 | 系统参数可编�?|
| 操作日志 | 操作日志完整可查 |
| 文件上传 | 文件可正常上�?|

### 12.2 非功能验�?
| 检查项 | 标准 |
|--------|------|
| 编译通过 | `go build` 零错�?|
| 前端构建 | `npm run build` 零错�?|
| API 响应时间 | <200ms (mock数据) |
| 页面渲染 | 无白屏、无控制台错�?|
| 浏览器兼�?| Chrome/Edge 最新版�?|

---

## �? 启动方式

### 后端

```bash
cd D:\CodeByAi\cl_system\backend
# 修改 manifest/config/config.yaml 中的数据库连�?go mod tidy
go run main.go
# 服务启动�?http://localhost:8099
```

### 前端

```bash
cd D:\CodeByAi\cl_system\frontend
# 修改 .env 中的 VITE_API_BASE_URL
npm install
npm run dev
# 访问 http://localhost:3000
# 默认登录: admin / admin123
```
